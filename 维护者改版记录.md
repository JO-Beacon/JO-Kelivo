# JO-AIClient 维护者改版记录

本文件记录当前已经落地且需要长期保护的 JO-AIClient 差异、外部基座来源和兼容边界。它是维护清单，不是愿望清单。

## 1. 当前基线

- JO-AIClient 产品版本：`0.1.12+12`（待发布）。
- 当前实现基座：原版 Kelivo `1.2.4+68`。
- 来源仓库：`https://github.com/Chevey339/kelivo`。
- 来源 tag：`v1.2.4`。
- 来源 commit：`6a293ef085bd4830905b2e199908af396d86c075`。
- 许可证：GNU AGPL-3.0；继续保留原作者及贡献者归属。
- Dart package name：`Kelivo`，现有导入继续使用 `package:Kelivo/...`。

JO-AIClient 保持独立 Git 历史。外部基座是可替换的实现输入，不是 JO-AIClient 仓库的祖先或永久上游；替换基座不得隐式 rebase、桥接无关历史、改变 remote 或整树复制外部工作区。

Kelivo 1.2.1 和 1.2.2 的基座替换记录属于历史来源；当前 1.2.4 基座以来源仓库的 `v1.2.4` tag 和上方 commit 为准，不能让外部工作目录的后续主分支提交静默漂移。

## 2. 基座替换边界

### 2.1 接受的路径组

以下内容以 Kelivo 1.2.4 为当前架构和行为基准，并延续此前 1.2.2 引入的 SQLite/Drift、结构化 parts 等兼容约束，再按 JO-AIClient 要求做最小适配：

- `lib/`：SQLite/Drift 数据库、repository、结构化 `MessagePart`、备份恢复、迁移、provider、语音、MCP、记忆、搜索和 UI 主体。
- `test/`、`integration_test/`、`test_driver/`、`tool/`：对应上游实现的测试、恢复工具和性能基线。
- `android/`、`ios/`、`macos/`、`linux/`、`windows/`、`web/`：平台模板、插件接入和当前基座平台能力；应用身份部分由 JO-AIClient 覆盖。
- `dependencies/`：当前基座所需本地 path dependencies，包括 `downsize` 和更新后的 MCP client。
- `assets/`、`docs/`、`docx/`、`drift_schemas/`：实际被运行时、文档或数据库验证使用的基座资源。
- `pubspec.yaml`、`analysis_options.yaml`、`l10n.yaml` 等根级构建配置：以当前基座依赖和工具链为主，保留 JO-AIClient 版本、仓库隔离和发布规则。

### 2.2 JO-AIClient 所有或混合路径

以下内容不能被外部快照直接覆盖：

- `AGENTS.md`：仓库硬约束和可替换基座规则。
- `README.md`、本文件、`Release日志.md`：JO-AIClient 产品、维护和发布记录。
- `.github/workflows/**`、`scripts/windows/**`：JO-AIClient 发布矩阵、签名边界和产物命名。
- 平台应用标识、显示名、URL scheme、Windows 单实例标识、数据目录来源、About、更新源和 Release asset 匹配。
- `optimize_chat_archive/`：仅面向 JO-AIClient 0.1.5 及更早旧 `chats.json` 的独立工具。
- JO-AIClient 功能触及的数据库、provider、设置和 UI 文件：在当前基座架构上按需求回放，不恢复旧文件。

### 2.3 明确排除

- 外部 `.git/**`、`.dart_tool/**`、`build/**`、IDE 配置、个人环境文件、签名材料和 secrets。
- 外部仓库无关 workflow、发布文档和产品身份文件。
- `参考文件/**` 本身；它只读且不进入 Git。
- 上游已经删除且当前无引用、无迁移价值的旧资产或实现。

常规生成物必须由仓库命令生成。`lib/core/models/chat_message.g.dart` 是 1.2.2 上游继续维护的旧 Hive 读取适配器，field 2 到结构化正文的迁移映射必须保留，不能被无条件 build_runner 覆盖。

## 3. 当前架构与兼容边界

### 3.1 数据与消息

- 聊天运行时唯一真相是 SQLite/Drift；旧 Hive 只参与首次迁移，不继续双写。
- 消息正文和附件唯一真相是 `List<MessagePart>`；图片、文件、文本、推理和未知 part 按结构化数据处理。
- `[image:]` / `[file:]` 只用于旧数据和旧备份解码，以及迁移前恢复备份；JO-AIClient 当前正常运行和正常新备份不得写回 marker。
- 更新消息角色、正文或附件必须经过 repository 和事务路径，不能只修改内存对象。
- 长会话界面固定使用数据库分页懒加载，不再提供关闭开关；完整历史读取代码和测试仅作为内部受控能力保留，不把全历史永久塞入日常界面常驻内存。

### 3.2 基座能力保护

下列基座能力不得被旧 JO-AIClient 文件覆盖：

- SQLite 安装、观察、备份、恢复、迁移收尾和失败回滚。
- 结构化 parts、附件资产引用、未知或损坏 part 的保真处理。
- 旧 Hive/`chats.json` 清洗、损坏记录跳过、计数和恢复备份。
- 记忆系统、语音与 ASR、MCP OAuth、本地设备工具和平台权限。
- Azure TTS、Gboard 图片粘贴、豆包搜索、话题复制及既有 provider 修复。
- data URI 图片缓存字节上限、工具详情动画、Responses 推理与图片生成兼容、iOS 合规和 Markdown 修复。
- 上下文日志、日志脱敏和大载荷省略、流式聊天与增量 Markdown 性能优化。
- 删除消息后的版本组锚点、时间线定位、自动滚动和末端回复修复。
- 旧版记忆模式全局开关、无密钥 Firecrawl 搜索和基于 ID 的引用显示。

未被 JO-AIClient 适配触及的模块以当前基座标签源码及上游测试为主要证据；触及的模块必须运行上游测试和 JO-AIClient 差异测试。

## 4. JO-AIClient 身份与发布规则

### 4.1 应用与数据隔离

- Android namespace/application ID：`com.psyche.jokelivo`。
- iOS/macOS Bundle ID：`com.psyche.jokelivo` 及对应测试 target 变体。
- Windows/Linux binary：`jo_kelivo`。
- Linux application ID：`com.psyche.jokelivo`。
- Web、窗口、托盘和系统显示名：`JO-AIClient`。
- OAuth scheme 使用 JO-AIClient 标识；`kelivo-file:///` 和既有兼容 URI 标识继续保留。
- Windows 单实例 mutex：`JOKelivoMutex`，窗口唤起只匹配 JO-AIClient 实例。
- 运行时目录由 JO-AIClient 平台身份和 `path_provider` 路径解析得到；不得 fallback 到原版 Kelivo 数据目录。
- 桌面备份页和存储空间页保留两个不同入口，但都打开当前 JO-AIClient 主数据目录。

在 JO-AIClient 身份和数据目录恢复完成前，不得启动新基座或触发旧数据迁移。首次迁移试验不得直接使用用户真实数据。

### 4.2 About、更新和版本

- About 的 JO-AIClient 分区显示 `0.1.12+12`，原版归属分区显示基座 `1.2.4 / 68`。
- 更新检查先探测 `https://api.github.com/repos/JO-Beacon/JO-AIClient/releases/latest`；当前平台没有可用资产或该入口不可用时，回落到 `https://api.github.com/repos/JO-Beacon/JO-Kelivo/releases/latest`。当前正式发布资产仍由后者提供。
- 下一发布标签候选为完整产品版本 `0.1.12+12`；正式资产名也包含完整 Flutter 版本。
- 更新匹配只接受当前 tag 对应的 `JO-AIClient` 资产，拒绝原版资产、其他版本和 `.sha1`/`.sha256`。
- Android 优先 arm64-v8a，Windows 优先 setup 后 portable，Linux 优先 AppImage 后 deb/tar.gz。

### 4.3 发布矩阵

JO-AIClient 候选发布矩阵保持：

- Android：arm64-v8a、armeabi-v7a、x86_64 三个 release APK。
- Windows x64：portable ZIP、setup EXE。
- Linux x64：AppImage、tar.gz、deb。
- 每个二进制资产都有同名 `.sha256`。

非发布 `workflow_dispatch` 会给资产版本追加短 commit，以免测试 artifact 冒充正式资产；只有目标发布 tag 或显式 `publish_release=true` 且 `release_tag` 与目标版本一致时才进入 GitHub Release 步骤。Android 发布必须具备完整签名 secrets；本地 keystore 和 `key.properties` 永不提交。

Windows workflow 当前没有 Authenticode 签名步骤，因此不得声称 Windows EXE 已签名。发布前必须按实际产物记录签名状态。

Windows setup 必须包含简体中文安装消息。构建脚本优先使用 Inno Setup 自带的 `ChineseSimplified.isl`，runner 缺少时下载官方 Inno Setup 语言文件；获取失败必须使构建失败，不能静默退化为缺少中文界面的安装器。

## 5. JO-AIClient 功能处置

| 能力 | 当前处置 | 当前实现边界 |
| --- | --- | --- |
| 单条消息用户/模型身份切换 | 替换实现 | repository 事务更新角色；刷新、重启和导出后保持一致，parts 不丢失 |
| 新建/复制助手置顶 | 回放 | 适配当前 business repository 和排序模型，移动端与桌面端共用规则 |
| 长会话懒加载开关 | 退役 | 不恢复用户设置，聊天界面始终使用数据库分页；完整历史读取代码和测试仅作为内部受控能力保留 |
| 历史附件可视化编辑 | 替换实现 | 编辑 `MessagePart`，支持增删替换附件并保留未知 part |
| 宽屏聊天区域拉宽 | 回放 | 落在共享聊天内容层，不改写 desktop shell 路由 |
| 两个用户数据目录入口 | 回放 | 桌面备份页和存储空间页保留各自入口语义，目标均为 JO-AIClient 主目录 |
| 本地备份处理提示 | 新增并统一 | 桌面端原本没有本地导入/导出加载提示；现在与移动端共用不可误触关闭、任务结束后精确移除的加载任务机制 |
| DeepSeek 默认 Anthropic-compatible | 回放 | 新建默认配置为 `ProviderKind.claude` 和 `https://api.deepseek.com/anthropic` |
| DeepSeek 内置搜索 | 保留基座并接通 JO-AIClient 默认 | 使用当前基座的识别、`web_search_20250305`、流式解析和 `end_turn` 行为 |
| DeepSeek 显式 OpenAI-compatible `/v1` | 保留 | 只在用户明确配置时使用，不被新默认覆盖 |
| 供应商余额查询 | 回放 | 非 OpenAI-compatible 主端点不静默拼接相对余额路径 |
| DeepSeek Web/App 导入 | 新增 | 接受官方 ZIP 或从中解压得到的 `conversations.json`，重建消息分支、选中路径、思维链、附件和联网搜索片段；合并保留本地内容，覆盖只定点替换对应导入会话 |
| 旧存档优化工具 | 限定保留 | 只接受 JO-AIClient 0.1.5 及更早导出的旧 `chats.json`，拒绝 Hive、SQLite、其他备份和非目标 JSON |
| 用户消息图片位置开关 | 退役 | 采用当前基座固定显示规则；遗留 key 不得导致崩溃或重新出现无效 UI |
| 旧版记忆模式 | 保留基座 | 使用当前基座的全局开关；默认关闭，不删除旧版记忆页面和数据路径 |
| 请求日志和上下文日志 | 保留基座 | 新安装默认开启；流式响应分块不写入请求日志，日志内容经过密钥脱敏和大载荷省略 |

本地备份处理提示的历史分类必须保持准确：桌面端本地导入/导出此前没有该设计，因此属于新增功能；移动端导入已有加载提示，移动端导出也已有弹窗调用，但旧实现会在弹窗首帧绘制前启动任务，且 `LoadingDialogCard` 首帧透明，实际表现为没有提示，因此移动端导出属于 Bug 修复。当前统一由共享加载任务路由先完成可见首帧再执行任务，并只移除自身路由，成功、取消和异常路径均可收尾。

Windows 备份恢复还修复了另一条独立故障：导入完成自动重启后，启动恢复门禁会在首次 `runApp` 前校验和切换数据库、附件，原生窗口只能持续显示白屏；同时 `restart_app` 先启动替代进程再结束旧进程，会与 `JOKelivoMutex` 发生交接竞争。当前启动时先显示不依赖业务数据的恢复处理中界面，Windows runner 对单实例锁采用有界交接等待，不改变备份格式或恢复校验强度。

## 6. 不得恢复的旧实现

- Hive 作为聊天运行时数据库，或 SQLite/Hive 双写。
- 把附件重新编码进正文 marker 并与 parts 双写。
- 绕过 repository 直接修改消息角色、正文或版本顺序。
- 在日常聊天界面永久加载全部会话并绕开数据库窗口。
- 旧独立附件解析器作为日常数据真相，或要求新备份向 JO-AIClient 0.1.5 降级。
- 已退役的用户消息图片位置设置和渲染分支。

旧 `chats.json` 优化工具仍可修复历史导出文件，但它不能读写当前应用数据库，也不能接入启动、打开会话或备份导入流程。

## 7. 数据兼容

### 7.1 应用内升级

- 改名只改变显示名称，不改变现有安装使用的应用标识、数据目录或 `kelivo.db` 文件。
- 运行时数据以 SQLite/Drift 为唯一真相；旧 Hive 数据只在迁移阶段读取一次，不继续双写。
- 迁移开始前创建并校验恢复备份；无法解码的损坏记录会跳过并计数，迁移失败必须保留恢复备份和可诊断状态。

### 7.2 备份格式

- `.joaiclient` 是 JO-AIClient 默认对外导出的完整快照，包含设置、SQLite 聊天数据库和本地文件；恢复固定采用覆盖模式，不作为跨产品数据库交换格式。
- `.zip` 的 `kelivo-backup` v2 是共享备份契约，可按备份内容包含设置、聊天数据库和本地文件，并支持覆盖或合并恢复。
- 当前已验证 Kelivo `1.2.2` 共享备份可以导入，JO-AIClient 生成的共享备份符合 Kelivo `1.2.2` 的契约；兼容性只覆盖双方共同支持的数据。

### 7.3 兼容边界

- 可互操作内容包括会话、消息、版本关系、文本、图片和文件附件、助手及供应商。
- JO-AIClient 专属设置和字段不保证被原版保留；结构化消息部件不会为了旧版本而重新降级写回正文标记。
- Chatbox `1.22` 以下版本按 v1.21.1 树形 JSON 兼容导入，并保留消息分支、嵌套分叉和选择状态；Cherry Studio 和旧版 `chats.json` 仍是独立的导入或迁移路径，不代表双向兼容；不得直接复制另一产品的数据目录或数据库文件。
- DeepSeek Web/App 导入接受官方 ZIP，或该 ZIP 内解压得到的 `conversations.json`。合并模式保留本地内容并按 DeepSeek 会话 ID 去重；覆盖模式仅定点替换对应的 DeepSeek 导入会话，不得清空批次外会话或全局业务数据。

兼容测试分别覆盖旧数据迁移、Kelivo `1.2.2` 共享备份导入，以及 JO-AIClient 共享备份导出后的契约读取；测试验证的是上述边界，不把完整 `.joaiclient` 快照误写成原版通用格式。

## 8. 验证基线

### 8.1 上一版本验证记录

以下结果属于 JO-Kelivo 0.1.6+6 / Kelivo 1.2.1+64 的已发布验证记录，不代表 0.1.7+7 已自动通过：

- `flutter analyze`：通过。
- 完整 `flutter test`：2319 passed，6 skipped，0 failed。
- 备份文件专项：59 passed，1 skipped，0 failed。
- parts/provider 专项：110 passed。
- 平台/MCP 专项：96 passed。
- Windows 适配专项：111 passed，2 skipped。
- MCP path dependency：`dart analyze` 通过；VM 测试 98 passed。默认 `dart test` 需要 Chrome，本机未覆盖浏览器平台。
- `dependencies/downsize`：`dart analyze` 通过，3 tests passed。
- Windows 本地 release 构建、portable 和中文 setup 打包、SHA-256、内部 EXE 名称与版本信息：通过；本地主候选哈希为 portable `91da3fab...f529d7f`、setup `6eb3e4e6...982ddd`。
- Windows 实机已验证本地备份导入/导出处理提示，以及真实备份导入、自动重启、恢复事务提交和正常进入应用；恢复结果为 `committed`，没有活动恢复标记残留。
- PR 干净检出：生成空 fallback 后 analyze 和相关测试通过；发布 workflow 仍从 GitHub secret 注入 fallback。
- `desiredFileName.txt`：`{}`。

- GitHub Actions Linux run `31812698493`：成功生成 AppImage、tar.gz、deb 和全部 `.sha256`，Release 步骤 skipped；三个二进制哈希分别为 `af014436...8023`、`717b5221...af7a`、`7e9f4207...04bf`。deb 身份为 `jo-kelivo` / `0.1.6` / `amd64`，入口、desktop 文件和图标存在。
- GitHub Actions Android run `31822982165`：成功生成 arm64-v8a、armeabi-v7a、x86_64 三个拆分 APK 和全部 `.sha256`，Release 步骤 skipped；包名 `com.psyche.jokelivo`、应用名 `JO-Kelivo`、版本 `0.1.6`、ABI 均正确，三个 APK 哈希分别为 `d68cfb16...d341`、`9044f2ca...642f`、`8b1b353c...24af`。
- GitHub Actions Android 签名验证 run `31827894964`：非发布模式成功生成三个签名 APK 和全部 `.sha256`，Release 步骤 skipped；三个 APK 的发布证书 SHA-256 均为 `81902bff...53c7d`，与 JO-Kelivo 0.1.5 一致，包名、版本和 ABI 均正确。仓库已配置完整发布签名 secrets；workflow 仍会在缺少任一签名项时拒绝正式发布。
- GitHub Actions Windows run `31823269926`：成功生成 portable、中文 setup 和全部 `.sha256`，Release 步骤 skipped；哈希分别为 `14357425...9ccc`、`1dcf8c74...2f45`，内部 `jo_kelivo.exe` 的产品名及 `0.1.6+6` 版本信息正确。
- Windows Actions 首次 run `31822982683` 暴露了干净 checkout 中 `lib/secrets/` 不存在的问题；提交 `79f4645` 让注入步骤显式创建目录，随后重跑通过。Android/Linux 原本已经显式创建该目录，无需修改。
- workflow bootstrap PR #1 已合入 `main`（merge commit `e1ca6ed`），使默认分支注册 Android、Windows、Linux 和 PR Checks；三套候选构建均已验证 artifact 上传且未创建 GitHub Release。

上一版本发布候选边界：

- Android、Windows、Linux 的完整候选资产矩阵已通过 release 级非发布构建、命名、SHA-256 和 artifact 上传验证。本轮不创建正式 tag 或 GitHub Release；非发布资产不能冒充正式发布件。
- Windows 主 EXE 和安装器均未做 Authenticode 签名。
- iOS、macOS、Web 不是本轮候选发布平台，未做 release 构建。
- 未使用真实 DeepSeek key 做在线冒烟；强制协议验收使用本地模拟服务完成。
- JO-Kelivo 0.1.6 与原版 Kelivo 1.2.1 的备份编排、备份模型、业务恢复和业务设置路由文件已逐项通过 SHA-256 一致性核对；两个方向的合成契约验收均已通过。共享会话、消息、版本关系、结构化附件、助手、供应商和附件文件保持可读，JO-Kelivo 本地专属显示设置不进入共享备份。

上述未覆盖项是上一版本的发布边界，不得直接当作本版本验证结果复用。

### 8.2 0.1.7+7 候选验证

截至 2026-08-15，本轮本地验证结果：

- `flutter pub get`、`flutter gen-l10n` 和 `flutter analyze`：通过；`desiredFileName.txt` 为 `{}`。
- 完整 `flutter test`：2465 passed，6 skipped，0 failed。
- `dependencies/gpt_markdown`：144 passed，6 skipped，0 failed。
- Kelivo 1.2.2 导入、JO-Kelivo 0.1.7 导出、旧数据迁移、应用身份和发布契约专项：108 passed，1 skipped，0 failed。
- `optimize_chat_archive`：19 passed，0 failed。
- Windows x64 本地 release 构建：通过，生成 `build/windows/x64/runner/Release/jo_kelivo.exe`；`webview_windows` 仅输出既有 CMake developer warning。
- Android Actions run `31864613635`、Linux Actions run `31864613435`、Windows Actions run `31864613474` 均通过，head SHA 均为 `f1ed58e104ee5cabd1e9a6bb4ebba5870593a8e5`；三套非发布 workflow 均完成候选资产和 SHA-256 上传，`Publish GitHub Release` 步骤均为 `skipped`。
- 三个候选 artifact ZIP 的 SHA-256 已与 GitHub artifact 元数据逐项核对一致；其中 8 个候选二进制的文件名、SHA-256 和目标平台矩阵均已核验。
- Android 三个拆分 APK 的包名为 `com.psyche.jokelivo`、应用名为 `JO-Kelivo`、版本名为 `0.1.7`，各自只包含目标 ABI，均通过 APK Signature Scheme v2；证书 SHA-256 为 `81902bff0923c1202f1a29648e25ba87e3a24499ee3f613545750a4a06353c7d`，与 JO-Kelivo 0.1.5 发布证书一致。
- Windows portable 内的 `jo_kelivo.exe` 产品名、版本和原始文件名正确；setup 的 ProductVersion 为 `0.1.7+7_f1ed58e`，简体中文消息文件已被 workflow 正确读取并通过 Inno Setup 编译。portable EXE 与 setup EXE 均未做 Authenticode 签名，符合当前发布边界。
- Linux AppImage 为 x86-64 ELF；tar.gz 包含根入口 `jo_kelivo`；deb 为 `Package: jo-kelivo`、`Version: 0.1.7`、`Architecture: amd64`，并包含应用入口、desktop 文件和 256x256 图标。
- 发布前发现三份 workflow 的 tag 过滤器仍为旧 `v*` 规则，无法响应项目约定的完整版本标签。三份过滤器已同步改为完整版本格式，并由发布契约测试防止退化。
- 正式标签 `0.1.7+7` 指向 `8c55564a1d3e8718f73e6f6fd6bdcc93bfd79295`。Android Actions run `31866176915`、Linux Actions run `31866176941`、Windows Actions run `31866176903` 均成功。
- 发布前草稿 Release 的标签和标题均为 `0.1.7+7`，正文与 `.github/release-notes/0.1.7+7.md` 一致；8 个正式二进制和 8 个 `.sha256` 已完整配对，校验文件内容与 GitHub 资产 digest 交叉核对一致。
- 正式 Android armeabi-v7a APK 已通过 APK Signature Scheme v2 验证，证书 SHA-256 仍为 `81902bff0923c1202f1a29648e25ba87e3a24499ee3f613545750a4a06353c7d`；三个 APK 由同一次构建使用同一 keystore 生成。

正式发布使用 `0.1.7+7` tag。三个平台 workflow 先创建或更新同一草稿 Release，全部资产核验完成后再人工公开；本轮 Release 已于 2026-08-15 公开，并确认 `releases/latest` 指向 `0.1.7+7`。Windows 产物继续明确为未做 Authenticode 签名。

### 8.3 0.1.11+11 已发布验证记录

- `0.1.11+11` 基于 Kelivo `1.2.4+68`，正式标签指向 `32304e24902d2c23c209480e05337b60397c93a4`；GitHub Release 已于 2026-09-01（北京时间）公开，共包含 18 个发布资产。
- `flutter analyze`：通过。
- 完整 `flutter test`：2997 passed，8 skipped，0 failed；Windows 回环测试使用进程级 `NO_PROXY/no_proxy`，未修改全局代理配置。
- 当前提交 `818ee551` 已落地上下文树完整性诊断与持久化校验、助手业务数据库目录和批量管理、头像非破坏性变换，以及相关桌面端和移动端交互适配。
- `.github/release-notes/0.1.6+6.md` 至 `.github/release-notes/0.1.11+11.md` 保存对应版本的 GitHub Release 正文。

### 8.4 0.1.12+12 当前开发记录

- 当前代码版本为 `0.1.12+12`，上游基座为 Kelivo `1.2.4+68`；GitHub 上最新已公开 Release 为 `0.1.11+11`，因此 `0.1.12+12` 的发布正文只记录相对 `0.1.11+11` 的增量。
- DeepSeek Web/App 导入已实装，接受官方 ZIP 或解压后的 `conversations.json`，重建消息分支、选择状态、思维链、附件和联网搜索片段，并使用共享可取消 isolate 执行文件解析。
- Chatbox `<1.22` 导入补齐树形分叉、供应商、助手分组、星标和已删除供应商恢复；消息菜单区分删除当前分支、删除节点以及删除消息和后续内容。
- 助手本地资产复制与清理、头像非方形裁剪、侧边栏多选返回和结构变更后的滚动取消均增加了对应需求测试。
- `.github/release-notes/0.1.12+12.md` 是当前待发布正文；发布前分析、完整测试和平台构建结果尚未写入本节，不得据此视为已经通过发布验证。

## 9. 后续维护检查

每次替换基座或修改相关模块时至少检查：

1. 基座来源、版本、commit 和许可证记录是否准确，JO-AIClient 历史和 remote 是否保持独立。
2. 平台身份、数据目录、OAuth scheme、Windows 单实例、托盘、About 和更新源是否仍为 JO-AIClient。
3. 聊天运行时是否仍只有 SQLite/Drift 一套真相，消息是否仍以 parts 为真相。
4. JO-AIClient 功能处置表中的回放、替换和退役结论是否被代码与需求测试保护。
5. 四个 ARB、生成文件、path dependencies 和相似 workflows 是否按 `AGENTS.md` 同步验证。
6. 发布资产、`.sha256`、tag、版本、签名状态和更新匹配是否与实际产物一致。
7. 参考快照、secrets、签名材料和构建产物是否未进入暂存区。

长期记录只写已经实现并验证的事实。新能力实装或边界变化后，必须同步更新功能处置、兼容说明和发布记录；尚未实现的事项留在本地计划或独立 issue，不得提前写成产品能力。
