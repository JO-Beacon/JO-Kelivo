# JO-Kelivo维护者改版记录

以下内容用于后续同步原版代码时保护本改版功能，普通用户可跳过。

## 改版内容

### 1. [`README.md`](README.md) 本身

- 如果再次同步参考源码，必须保留本文件改版描述。
- 可以补充新的改版保护项，但不要删除已有保护项，除非明确确认该改动已废弃。

### 2. 单条聊天记录身份切换

这是主项目保留的自定义功能之一。

功能目标：

- 在单条聊天消息的更多操作菜单中，允许把一条消息的身份在“用户”和“模型”之间切换。
- 用户消息可以切换为模型消息。
- 模型消息可以切换为用户消息。
- 切换只影响当前这一条消息，不应该批量改动整段对话。

保护范围：

- 菜单入口：[`lib/features/chat/widgets/message_more_sheet.dart`](lib/features/chat/widgets/message_more_sheet.dart)
- 消息列表回调：[`lib/features/home/widgets/message_list_view.dart`](lib/features/home/widgets/message_list_view.dart)
- 页面控制器入口：[`lib/features/home/controllers/home_page_controller.dart`](lib/features/home/controllers/home_page_controller.dart)
- 聊天控制器更新：[`lib/features/home/controllers/chat_controller.dart`](lib/features/home/controllers/chat_controller.dart)
- 底层消息保存：[`lib/core/services/chat/chat_service.dart`](lib/core/services/chat/chat_service.dart)
- 本地化文案：[`lib/l10n/app_en.arb`](lib/l10n/app_en.arb)、[`lib/l10n/app_zh.arb`](lib/l10n/app_zh.arb)、[`lib/l10n/app_zh_Hans.arb`](lib/l10n/app_zh_Hans.arb)、[`lib/l10n/app_zh_Hant.arb`](lib/l10n/app_zh_Hant.arb)

同步或重构时的检查点：

- 更多菜单里仍然能看到“切换为用户”和“切换为模型”。
- 切换后消息角色会真实保存，而不是只改界面显示。
- `role` 更新链路不能被回退成只能更新 `content`。
- 新增或修改本地化 key 时，四个 ARB 文件必须保持同步。

### 3. 新建或复制助手时插到顶部的设置项

这是主项目保留的自定义功能之一。

功能目标：

- 设置里有一个开关，用来控制“新建助手”和“复制助手”放到助手列表顶部。
- 开关关闭时，保持默认行为。
- 开关打开时，新建或复制出来的助手插入列表顶部。
- 移动端和桌面端都必须生效。

保护范围：

- 设置持久化：[`lib/core/providers/settings_provider.dart`](lib/core/providers/settings_provider.dart)
- 助手新增/复制逻辑：[`lib/core/providers/assistant_provider.dart`](lib/core/providers/assistant_provider.dart)
- 移动端助手页面：[`lib/features/assistant/pages/assistant_settings_page.dart`](lib/features/assistant/pages/assistant_settings_page.dart)
- 桌面端助手设置页：[`lib/desktop/setting/assistants_pane.dart`](lib/desktop/setting/assistants_pane.dart)
- 移动端显示设置：[`lib/features/settings/pages/display_settings_page.dart`](lib/features/settings/pages/display_settings_page.dart)
- 桌面端显示设置：[`lib/desktop/setting/display_pane.dart`](lib/desktop/setting/display_pane.dart)
- 本地化文案：[`lib/l10n/app_en.arb`](lib/l10n/app_en.arb)、[`lib/l10n/app_zh.arb`](lib/l10n/app_zh.arb)、[`lib/l10n/app_zh_Hans.arb`](lib/l10n/app_zh_Hans.arb)、[`lib/l10n/app_zh_Hant.arb`](lib/l10n/app_zh_Hant.arb)

同步或重构时的检查点：

- 设置项不能消失。
- 设置值必须持久化，重启应用后不能丢。
- 移动端新建助手要读取这个设置。
- 移动端复制助手要读取这个设置。
- 桌面端新建助手要读取这个设置。
- 桌面端复制助手要读取这个设置。
- `addAssistant` 和 `duplicateAssistant` 的插入位置参数不能被删掉。
- 新增或修改本地化 key 时，四个 ARB 文件必须保持同步。

### 4. 长会话版本消息渲染顺序修复

这是主项目保留的 BUG 修复之一。

问题现象：

- 打开已有长会话，或在长会话里向上/向下滚动加载历史时，界面可能看起来随机插入了旧上下文。
- 这个问题主要出现在含有编辑、重新生成、分支版本消息的会话里。
- 模型实际收到的上下文通常是正常的，问题发生在聊天记录展示层。

原因简述：

- 编辑或重新生成消息时，新版本可能会被追加到会话尾部。
- 长会话懒加载只会加载当前窗口附近的一段消息。
- 如果当前窗口只加载到了“旧消息的新版本”，但没有加载到它原本所属的消息组位置，界面就可能把它误显示到当前上下文中。

修复策略：

- 第一层是展示层兼容：不迁移、不重写用户旧存档。
- [`ChatController.collapseVersions()`](lib/features/home/controllers/chat_controller.dart) 折叠版本消息时，必须按持久化会话里的原始消息位置稳定排序。
- 当前懒加载窗口只应显示原始组锚点也在当前窗口内的版本组，避免旧消息的新版本被误插入到当前可见上下文。
- 第二层是新写入源头修复：[`ChatService.appendMessageVersion()`](lib/core/services/chat/chat_service.dart) 和 [`ChatService.addMessage()`](lib/core/services/chat/chat_service.dart) 对 `groupId + version > 0` 的新版本消息，必须插回同组消息附近，而不是继续追加到会话尾部。
- 普通新消息仍追加到尾部；找不到同组锚点时也应回退追加到尾部，避免破坏写入流程。
- 旧存档彻底重排不属于应用自动迁移范围。如需清理旧 `chats.json`，必须使用完全独立的修复工具并保留备份。
- 删除版本分支后必须按懒加载开关同步当前消息列表：懒加载关闭时，[`ChatController.reloadMessages()`](lib/features/home/controllers/chat_controller.dart) 必须重新读取完整会话；懒加载开启时，仍保持当前有界窗口，不能意外全量展开历史。
- 单版本删除和删除全部版本都会经过 [`HomeViewModel._deleteMessageVersions()`](lib/features/home/controllers/home_view_model.dart)，最终统一调用 [`ChatController.reloadMessages()`](lib/features/home/controllers/chat_controller.dart)，不能绕开这条同步链路。

第二阶段独立工具：

- 独立工具目录：[`repair_chat_archive/`](repair_chat_archive/)
- 主程序：[`repair_chat_archive/repair_chat_archive.py`](repair_chat_archive/repair_chat_archive.py)
- 工具说明：[`repair_chat_archive/README.md`](repair_chat_archive/README.md)
- 测试文件：[`repair_chat_archive/tests/test_repair_chat_archive.py`](repair_chat_archive/tests/test_repair_chat_archive.py)
- 运行方式：进入 [`repair_chat_archive/`](repair_chat_archive/) 后执行 `uv run python repair_chat_archive.py path/to/chats.json`。
- 默认输出：`chats.fixed.json`。
- 默认备份：`chats.backup.json`。
- 该工具只使用 Python 标准库，不导入 Kelivo 的 [`lib/`](lib/) 代码，不依赖 Flutter / Dart，不修改 Kelivo 本地数据库。
- 该工具只重排 `Conversation.messageIds`，不修改消息正文、消息 ID、版本号、时间戳、会话 ID、`versionSelections` 或备份 schema。
- 遇到重复 `messageIds`、缺失消息、找不到非版本锚点、JSON 结构异常时，必须保守跳过并打印报告，不能强行改坏用户存档。

保护范围：

- 展示层折叠逻辑：[`lib/features/home/controllers/chat_controller.dart`](lib/features/home/controllers/chat_controller.dart)
- 长会话懒加载与版本消息回归测试：[`test/features/home/controllers/chat_controller_lazy_history_test.dart`](test/features/home/controllers/chat_controller_lazy_history_test.dart)
- 底层版本写入顺序：[`lib/core/services/chat/chat_service.dart`](lib/core/services/chat/chat_service.dart)
- 底层版本写入顺序测试：[`test/core/services/chat/chat_service_temporary_conversation_test.dart`](test/core/services/chat/chat_service_temporary_conversation_test.dart)
- 第二阶段独立旧存档修复工具：[`repair_chat_archive/`](repair_chat_archive/)

同步或重构时的检查点：

- 长会话尾部窗口不能显示原始组锚点不在窗口内的旧消息编辑版本。
- 完整会话折叠结果仍要把选中的编辑版本放回原始消息位置。
- 编辑旧消息产生的新版本，不能被写到会话尾部。
- 重新生成旧助手消息产生的新版本，不能被写到会话尾部。
- 普通新消息仍应写到会话尾部，不能被错误插入旧消息组。
- 找不到同组锚点的版本消息应安全回退追加到尾部。
- 模型请求上下文和 UI 展示顺序不能混为一谈。
- 不要在应用启动、打开会话或导入备份时静默重排旧存档。
- 关闭懒加载后删除旧分支，聊天列表必须立即保留删除后的完整会话，不能只显示删除前局部窗口，也不能依赖切换会话后才恢复。
- 开启懒加载后删除旧分支，聊天列表必须继续保持有界窗口，不能为了修复删除同步而回退成全量渲染长会话。
- 修改相关 Dart 逻辑后，至少运行 [`test/features/home/controllers/chat_controller_lazy_history_test.dart`](test/features/home/controllers/chat_controller_lazy_history_test.dart) 和 [`test/core/services/chat/chat_service_temporary_conversation_test.dart`](test/core/services/chat/chat_service_temporary_conversation_test.dart)。
- 修改独立 Python 工具后，进入 [`repair_chat_archive/`](repair_chat_archive/) 运行 `uv run --with pytest pytest` 和 `python -m py_compile repair_chat_archive.py tests\\test_repair_chat_archive.py`。

### 5. DeepSeek Anthropic 通道与原生搜索

这是主项目保留的自定义模型接入与 BUG 修复之一。

功能目标：

- 默认 DeepSeek 提供商走 Anthropic 兼容通道，而不是 OpenAI 兼容 `/v1` 通道。
- DeepSeek 默认 base URL 必须保持为 `https://api.deepseek.com/anthropic`。
- DeepSeek V4 支持普通模型内置搜索，对应 Anthropic `web_search_20250305`。
- DeepSeek V4 不支持“模型内置搜索(新)”，不能显示或注入 `web_search_20260209`。
- DeepSeek 搜索完成并返回 `end_turn` 后必须结束当前轮，不能因为出现 server tool 就继续发起下一轮请求。

保护范围：

- 默认 provider 类型与 base URL：[`lib/core/providers/settings_provider.dart`](lib/core/providers/settings_provider.dart)
- 内置搜索支持矩阵：[`lib/core/services/api/builtin_tools.dart`](lib/core/services/api/builtin_tools.dart)
- Anthropic/Claude 流式 server tool 停止逻辑：[`lib/core/services/api/providers/claude_official.dart`](lib/core/services/api/providers/claude_official.dart)
- DeepSeek 默认配置、余额边界与搜索回归测试：[`test/provider_balance_service_test.dart`](test/provider_balance_service_test.dart)、[`test/claude_thinking_compat_test.dart`](test/claude_thinking_compat_test.dart)

同步或重构时的检查点：

- [`ProviderConfig.classify()`](lib/core/providers/settings_provider.dart) 对 DeepSeek 必须返回 Claude/Anthropic 类型。
- [`ProviderConfig._defaultBase()`](lib/core/providers/settings_provider.dart) 对 DeepSeek 必须返回 `https://api.deepseek.com/anthropic`。
- [`BuiltInToolsHelper.isClaudeBuiltInSearchSupportedModel()`](lib/core/services/api/builtin_tools.dart) 必须包含 `deepseek-v4-pro` 和 `deepseek-v4-flash`。
- [`BuiltInToolsHelper.isClaudeDynamicWebSearchSupportedModel()`](lib/core/services/api/builtin_tools.dart) 不能包含 DeepSeek V4，避免 UI 显示“模型内置搜索(新)”。
- DeepSeek 普通内置搜索即使配置里残留 `toolVersion: web_search_20260209`，也必须降回 `web_search_20250305`，不能额外注入 `code_execution_20250825`。
- [`_sendClaudeStream()`](lib/core/services/api/providers/claude_official.dart) 只能在 `stop_reason == pause_turn` 时续轮；`server_tool_use` + `web_search_tool_result` + `end_turn` 必须直接完成，不能重复搜索或继续深度思考。
- DeepSeek Anthropic 默认配置不应默认启用 OpenAI 兼容余额查询。
- 修改相关逻辑后，至少运行 [`test/claude_thinking_compat_test.dart`](test/claude_thinking_compat_test.dart)、[`test/provider_balance_service_test.dart`](test/provider_balance_service_test.dart) 和 `flutter analyze`。

### 6. 历史消息附件可视化编辑

这是主项目保留的自定义体验修复之一。

问题现象：

- 发送前，输入框里的图片和文件有可视化附件 UI。
- 发送后再编辑历史消息时，原本的图片和文件会退化成文本标记或链接。
- 用户想删除、替换或继续添加图片时，需要手动处理文件路径，体验很差。

修复策略：

- 编辑历史消息时，必须先从消息正文中解析附件标记，而不是把附件标记直接放进文本框。
- 文本框只展示可编辑正文。
- 图片和文件必须显示在独立附件区。
- 附件区支持删除已有图片/文件、继续添加图片/文件，以及替换单张图片。
- 保存时仍按现有持久化格式组装回 `[image:path]` 和 `[file:path|name|mime]`，保持旧聊天记录兼容。
- 编辑保存仍沿用现有版本机制：创建新消息版本，不直接覆盖旧版本。

保护范围：

- 附件解析与组装：[`lib/features/chat/utils/message_attachment_parser.dart`](lib/features/chat/utils/message_attachment_parser.dart)
- 编辑结果模型：[`lib/features/chat/models/message_edit_result.dart`](lib/features/chat/models/message_edit_result.dart)
- 附件编辑组件：[`lib/features/chat/widgets/message_attachment_editor.dart`](lib/features/chat/widgets/message_attachment_editor.dart)
- 移动端编辑 Sheet：[`lib/features/chat/widgets/message_edit_sheet.dart`](lib/features/chat/widgets/message_edit_sheet.dart)
- 桌面端编辑弹窗：[`lib/desktop/message_edit_dialog.dart`](lib/desktop/message_edit_dialog.dart)
- 编辑保存入口：[`lib/features/home/controllers/home_page_controller.dart`](lib/features/home/controllers/home_page_controller.dart)
- 底层版本写入：[`lib/core/services/chat/chat_service.dart`](lib/core/services/chat/chat_service.dart)
- 回归测试：[`test/core/services/chat/chat_service_temporary_conversation_test.dart`](test/core/services/chat/chat_service_temporary_conversation_test.dart)
- 本地化文案：[`lib/l10n/app_en.arb`](lib/l10n/app_en.arb)、[`lib/l10n/app_zh.arb`](lib/l10n/app_zh.arb)、[`lib/l10n/app_zh_Hans.arb`](lib/l10n/app_zh_Hans.arb)、[`lib/l10n/app_zh_Hant.arb`](lib/l10n/app_zh_Hant.arb)

同步或重构时的检查点：

- 编辑含图片的历史用户消息时，图片不能只显示为 `[image:...]` 文本。
- 编辑含文件的历史用户消息时，文件不能只显示为 `[file:...]` 文本。
- 删除附件后保存，新版本消息中不能继续包含被删掉的附件标记。
- 替换图片后保存，新版本消息中必须使用替换后的图片路径。
- 添加图片或文件后保存，新版本消息必须继续使用现有 `[image:path]` / `[file:path|name|mime]` 格式。
- “保存并发送”必须使用编辑后的正文和附件重新生成回复。
- 旧版本消息仍应保留，不能把编辑行为改成直接覆盖原消息。
- 附件选择和复制应继续复用 [`FileImportHelper`](lib/utils/file_import_helper.dart) 与应用上传目录，不能直接引用临时选择路径作为长期存档。
- 新增或修改附件编辑文案时，四个 ARB 文件必须同步，并运行 `flutter gen-l10n`。
- 修改相关逻辑后，至少运行 [`test/core/services/chat/chat_service_temporary_conversation_test.dart`](test/core/services/chat/chat_service_temporary_conversation_test.dart)、`flutter analyze` 和相关平台验证。

### 7. 用户消息图片分离显示设置

这是主项目保留的自定义体验设置之一。

功能目标：

- 用户消息中的上传图片默认仍显示在消息气泡内，保持旧用户默认体验不变。
- 移动端“显示设置 → 聊天项目显示”和桌面端“设置 → 显示 → Chat Item Display”里都有“分离显示用户消息图片”开关，可选择把用户消息图片显示到气泡下方的独立区域。
- 该设置只影响历史消息展示层，不改变消息正文、附件标记、编辑保存、重新发送或 API 构造。
- 开关值必须持久化，重启应用后不能丢。

兼容边界：

- 默认值必须是关闭，旧配置没有该 key 时应回退到原来的气泡内显示。
- 聊天消息仍必须使用现有 `[image:path]` 持久化格式，不能为了视觉美化新增存档 schema。
- 旧版本应用不认识该设置时应可安全忽略，不影响读取聊天记录。
- 新版本读取旧聊天记录时不需要迁移，仍从消息正文解析图片标记。

保护范围：

- 设置持久化：[`lib/core/providers/settings_provider.dart`](lib/core/providers/settings_provider.dart)
- 移动端显示设置入口：[`lib/features/settings/pages/display_settings_page.dart`](lib/features/settings/pages/display_settings_page.dart)
- 桌面端显示设置入口：[`lib/desktop/setting/display_pane.dart`](lib/desktop/setting/display_pane.dart)
- 用户消息展示：[`lib/features/chat/widgets/chat_message_widget.dart`](lib/features/chat/widgets/chat_message_widget.dart)
- 回归测试：[`test/features/chat/widgets/chat_message_widget_background_test.dart`](test/features/chat/widgets/chat_message_widget_background_test.dart)
- 本地化文案：[`lib/l10n/app_en.arb`](lib/l10n/app_en.arb)、[`lib/l10n/app_zh.arb`](lib/l10n/app_zh.arb)、[`lib/l10n/app_zh_Hans.arb`](lib/l10n/app_zh_Hans.arb)、[`lib/l10n/app_zh_Hant.arb`](lib/l10n/app_zh_Hant.arb)

同步或重构时的检查点：

- “分离显示用户消息图片”设置项不能从移动端或桌面端显示设置里消失。
- 设置值必须通过 [`SettingsProvider`](lib/core/providers/settings_provider.dart) 持久化，重启应用后不能丢。
- 默认关闭时，图片仍应显示在用户消息气泡内。
- 开启后，图片应显示在用户消息气泡下方独立区域，并且点击预览仍能打开图片查看页。
- 该设置不能改动 `[image:path]` / `[file:path|name|mime]` 持久化格式。
- 新增或修改本地化 key 时，四个 ARB 文件必须同步，并运行 `flutter gen-l10n`。
- 修改相关逻辑后，至少运行 [`test/features/chat/widgets/chat_message_widget_background_test.dart`](test/features/chat/widgets/chat_message_widget_background_test.dart)、[`test/core/services/chat/chat_service_temporary_conversation_test.dart`](test/core/services/chat/chat_service_temporary_conversation_test.dart) 和 `flutter analyze`。

### 8. 应用身份独立化

这是主项目保留的跨平台应用身份改版之一。

功能目标：

- JO-Kelivo 必须在系统层面与原版 Kelivo 形成不同应用身份，支持与原版并存安装、并存启动和分别卸载。
- Android、iOS、macOS、Windows、Linux、Web 的用户可见名称应使用 JO-Kelivo，而不是继续显示为原版 Kelivo / kelivo。
- 平台包名、Bundle ID、GTK Application ID、可执行文件名、安装器 AppId、Linux 包名和桌面入口等系统识别字段不能与原版 Kelivo 共用。
- 只保留 Dart 包名 `Kelivo` 作为源码导入兼容事实，不要把 `package:Kelivo/...` 改成 `package:JO_Kelivo/...` 或其他名称。

差异：

- Android：原版 [`android/app/build.gradle.kts`](参考文件/原版kelivo1.1.15/kelivo-1.1.15/android/app/build.gradle.kts) 使用 `com.psyche.kelivo`，本项目 [`android/app/build.gradle.kts`](android/app/build.gradle.kts) 改为 `com.psyche.jokelivo`，同时覆盖 `namespace` 和 `applicationId`。
- iOS：原版 [`ios/Runner.xcodeproj/project.pbxproj`](参考文件/原版kelivo1.1.15/kelivo-1.1.15/ios/Runner.xcodeproj/project.pbxproj) 使用 `com.psyche.kelivo` / `com.psyche.kelivo.RunnerTests`，本项目 [`ios/Runner.xcodeproj/project.pbxproj`](ios/Runner.xcodeproj/project.pbxproj) 改为 `com.psyche.jokelivo` / `com.psyche.jokelivo.RunnerTests`；原版 [`ios/Runner/Info.plist`](参考文件/原版kelivo1.1.15/kelivo-1.1.15/ios/Runner/Info.plist) 显示名为 `Kelivo`、包名显示为 `kelivo`，本项目 [`ios/Runner/Info.plist`](ios/Runner/Info.plist) 改为 `JO-Kelivo`。
- macOS：原版 [`macos/Runner/Configs/AppInfo.xcconfig`](参考文件/原版kelivo1.1.15/kelivo-1.1.15/macos/Runner/Configs/AppInfo.xcconfig) 使用 `PRODUCT_NAME = kelivo` 和 `PRODUCT_BUNDLE_IDENTIFIER = com.psyche.kelivo`，本项目 [`macos/Runner/Configs/AppInfo.xcconfig`](macos/Runner/Configs/AppInfo.xcconfig) 改为 `PRODUCT_NAME = JO-Kelivo` 和 `PRODUCT_BUNDLE_IDENTIFIER = com.psyche.jokelivo`；原版 [`macos/Runner.xcodeproj/project.pbxproj`](参考文件/原版kelivo1.1.15/kelivo-1.1.15/macos/Runner.xcodeproj/project.pbxproj) 产物与测试宿主为 `kelivo.app` / `kelivo`，本项目 [`macos/Runner.xcodeproj/project.pbxproj`](macos/Runner.xcodeproj/project.pbxproj) 改为 `JO-Kelivo.app` / `JO-Kelivo`，测试 Bundle ID 也改为 `com.psyche.jokelivo.RunnerTests`。
- Windows：原版 [`windows/CMakeLists.txt`](参考文件/原版kelivo1.1.15/kelivo-1.1.15/windows/CMakeLists.txt) 构建 `kelivo.exe`，本项目 [`windows/CMakeLists.txt`](windows/CMakeLists.txt) 改为 `jo_kelivo.exe`；原版 [`windows/runner/Runner.rc`](参考文件/原版kelivo1.1.15/kelivo-1.1.15/windows/runner/Runner.rc) 的资源名为 `kelivo` / `kelivo.exe`，本项目 [`windows/runner/Runner.rc`](windows/runner/Runner.rc) 改为 `JO-Kelivo` / `jo_kelivo.exe`；原版 [`windows/runner/main.cpp`](参考文件/原版kelivo1.1.15/kelivo-1.1.15/windows/runner/main.cpp) 使用 `KelivoMutex` 和窗口标题 `kelivo`，本项目 [`windows/runner/main.cpp`](windows/runner/main.cpp) 使用 `JOKelivoMutex` 和窗口标题 `JO-Kelivo`。
- Windows 安装器：原版 workflow 内联安装器使用 `Kelivo`、`kelivo.exe`、`{{A7B8C9D0-E1F2-4A5B-8C9D-0E1F2A3B4C5D}}`，本项目 [`scripts/windows/kelivo_installer.iss`](scripts/windows/kelivo_installer.iss) 使用 `JO-Kelivo`、`jo_kelivo.exe`、`{{D4C6D2A7-8F3E-4D7B-9D55-6B6B6D2E5A91}}`，避免和原版共用安装目录、开始菜单组、桌面快捷方式目标和卸载注册身份。
- Linux：原版 [`linux/CMakeLists.txt`](参考文件/原版kelivo1.1.15/kelivo-1.1.15/linux/CMakeLists.txt) 使用 `BINARY_NAME = kelivo`、`APPLICATION_ID = com.psyche.kelivo`，本项目 [`linux/CMakeLists.txt`](linux/CMakeLists.txt) 改为 `jo_kelivo` 和 `com.psyche.jokelivo`；原版 [`linux/runner/my_application.cc`](参考文件/原版kelivo1.1.15/kelivo-1.1.15/linux/runner/my_application.cc) 使用图标名与窗口标题 `kelivo`，本项目 [`linux/runner/my_application.cc`](linux/runner/my_application.cc) 改为 `jo_kelivo` 和 `JO-Kelivo`；各 Linux 发布 workflow 中的 desktop 文件、包名、安装目录、符号链接和图标名也应保持 `jo_kelivo` / `JO-Kelivo`。
- Web：原版 [`web/manifest.json`](参考文件/原版kelivo1.1.15/kelivo-1.1.15/web/manifest.json) 使用 `kelivo`，本项目 [`web/manifest.json`](web/manifest.json) 改为 `JO-Kelivo`，避免 PWA 安装名继续显示原版名称。
- Release 与 CI：原版 workflow 产物名以 `Kelivo_...` 开头，本项目 workflow 和发布规范应使用 `JO-Kelivo_...` 或本文件“Release 二进制文件命名规范”要求的 `JO-Kelivo-v{version}-...`，不能回退为原版文件名前缀。

保护范围：

- Android 应用 ID 与 namespace：[`android/app/build.gradle.kts`](android/app/build.gradle.kts)
- iOS 显示名与 Bundle ID：[`ios/Runner/Info.plist`](ios/Runner/Info.plist)、[`ios/Runner.xcodeproj/project.pbxproj`](ios/Runner.xcodeproj/project.pbxproj)
- macOS 产品名、Bundle ID、app 产物名与测试宿主：[`macos/Runner/Configs/AppInfo.xcconfig`](macos/Runner/Configs/AppInfo.xcconfig)、[`macos/Runner.xcodeproj/project.pbxproj`](macos/Runner.xcodeproj/project.pbxproj)
- Windows 可执行文件名、资源元数据、窗口标题和单实例身份：[`windows/CMakeLists.txt`](windows/CMakeLists.txt)、[`windows/runner/Runner.rc`](windows/runner/Runner.rc)、[`windows/runner/main.cpp`](windows/runner/main.cpp)
- Windows 安装器身份：[`scripts/windows/kelivo_installer.iss`](scripts/windows/kelivo_installer.iss)、[`scripts/windows/build_installer.ps1`](scripts/windows/build_installer.ps1)
- Linux 可执行文件名、GTK Application ID、窗口标题、图标名和发布包身份：[`linux/CMakeLists.txt`](linux/CMakeLists.txt)、[`linux/runner/my_application.cc`](linux/runner/my_application.cc)、[`.github/workflows`](.github/workflows)
- Web PWA 名称：[`web/manifest.json`](web/manifest.json)、[`web/index.html`](web/index.html)
- Release 文件名前缀与归档规范：[`维护者改版记录.md`](维护者改版记录.md)、[`Release日志.md`](Release日志.md)、[`.github/workflows`](.github/workflows)

同步或重构时的检查点：

- 同步原版平台目录后，不能把 `com.psyche.jokelivo` 覆盖回 `com.psyche.kelivo`。
- 不能把 `JO-Kelivo` 用户可见应用名覆盖回 `Kelivo` 或 `kelivo`。
- 不能把 Windows / Linux 可执行文件名 `jo_kelivo` 覆盖回 `kelivo`。
- Windows 安装器 `AppId` 必须继续使用 JO-Kelivo 专属 GUID，不能和原版 Kelivo 共用。
- Linux `APPLICATION_ID`、desktop 文件名、`Exec`、`Icon`、包名、安装目录和 `/usr/bin` 链接必须继续使用 JO-Kelivo 独立身份。
- macOS app 产物、测试宿主路径和 Bundle ID 必须同步改名，不能只改显示名。
- iOS Bundle ID 和 RunnerTests Bundle ID 必须同步改名，不能只改 [`ios/Runner/Info.plist`](ios/Runner/Info.plist) 显示名。
- Web PWA manifest 和 [`web/index.html`](web/index.html) 标题不能回退为原版 `kelivo`。
- 不要为应用身份独立化改 Dart 包名；[`pubspec.yaml`](pubspec.yaml) 的包名仍为 `Kelivo`，以保持既有 `package:Kelivo/...` 导入可用。
- 如触及平台目录或 workflow，必须检查所有相似 workflow，避免只改其中一个发布流程。
- 修改平台身份后，至少按受影响平台运行对应构建验证；无法验证的平台必须在交付说明中明确风险。

### 9. Windows 单实例窗口唤起隔离

这是主项目保留的 Windows 并存运行修复之一。

问题现象：

- 原版 Kelivo 和 JO-Kelivo 同时运行时，在 Windows 上双击 JO-Kelivo 快捷方式，可能会跳转到原版 Kelivo 窗口。
- 该问题会破坏“原版与 JO-Kelivo 可并存安装和使用”的应用身份独立化目标。

原因简述：

- Windows runner 的单实例唤起逻辑曾依赖通用 Flutter 窗口类和窗口标题查找已有窗口。
- 原版 Kelivo 与 JO-Kelivo 同时运行时，如果窗口类或标题重合，就可能把原版窗口误判为 JO-Kelivo 已有实例。
- 不能通过修改主窗口类名来隔离应用：[`bitsdojo_window`](dependencies/) 的 Windows 原生实现依赖默认 Flutter runner 窗口类 `FLUTTER_RUNNER_WIN32_WINDOW`，改掉窗口类会破坏 Windows 自定义标题栏/窗口初始化链路，可能导致白屏。

修复策略：

- Win32 runner 主窗口类必须保持 `FLUTTER_RUNNER_WIN32_WINDOW`，以兼容 Flutter Windows runner、`bitsdojo_window` 和相关窗口插件。
- JO-Kelivo 在创建主窗口后设置专属 Win32 窗口属性 `JOKelivoMainWindow`。
- 单实例唤起时枚举顶层窗口，只选择同时满足默认 Flutter 窗口类和 `JOKelivoMainWindow` 属性的窗口，不再依赖可能被 Dart 层或原版应用改写、重合的窗口标题。
- 命名互斥体仍使用 JO-Kelivo 自己的 `JOKelivoMutex`，用于限制 JO-Kelivo 自身多开，不应与原版 Kelivo 共用。

保护范围：

- Windows 启动入口与单实例互斥体：[`windows/runner/main.cpp`](windows/runner/main.cpp)
- Windows runner 窗口类与已有窗口查找逻辑：[`windows/runner/win32_window.cpp`](windows/runner/win32_window.cpp)
- Windows runner 单实例唤起接口声明：[`windows/runner/win32_window.h`](windows/runner/win32_window.h)

同步或重构时的检查点：

- [`windows/runner/win32_window.cpp`](windows/runner/win32_window.cpp) 中的窗口类必须保持 `FLUTTER_RUNNER_WIN32_WINDOW`，不能为了应用隔离改成私有类名。
- [`Win32Window::SendAppLinkToInstance()`](windows/runner/win32_window.cpp) 不能退回按窗口标题查找原版或通用 Flutter 窗口，必须使用 JO-Kelivo 专属窗口属性过滤。
- [`windows/runner/main.cpp`](windows/runner/main.cpp) 中的互斥体不能被覆盖回原版 Kelivo 的互斥体名称。
- 原版 Kelivo 正在运行时，启动 JO-Kelivo 必须打开或唤起 JO-Kelivo 自己的窗口，不能置前原版窗口。
- JO-Kelivo 已经运行时，再次启动 JO-Kelivo 仍应只唤起已有 JO-Kelivo 窗口，不能新开第二个主窗口。
- 修改 Windows runner 后，至少运行 `flutter build windows` 做 Windows 目标验证；如环境限制无法运行，必须说明未验证的风险。

### 10. Windows 安装器 Inno Setup 自动定位

这是主项目保留的 Windows 安装程序构建稳定性修复之一。

问题现象：

- 本机通过 `winget` 安装 Inno Setup 6 后，实际可能安装到用户级目录，而不是系统级目录。
- 如果安装器构建脚本只硬编码 `C:\Program Files (x86)\Inno Setup 6\ISCC.exe`，就会在用户级安装场景下找不到 `ISCC.exe`。

修复策略：

- [`scripts/windows/build_installer.ps1`](scripts/windows/build_installer.ps1) 默认不再硬编码单一路径。
- 未显式传入 `-InnoSetupCompiler` 时，脚本必须按顺序查找：
  1. `%LOCALAPPDATA%\Programs\Inno Setup 6\ISCC.exe`
  2. `C:\Program Files\Inno Setup 6\ISCC.exe`
  3. `C:\Program Files (x86)\Inno Setup 6\ISCC.exe`
- 如果三处都找不到，脚本必须给出明确错误，提示安装 Inno Setup 6 或显式传入 `-InnoSetupCompiler`。
- 仍保留 `-InnoSetupCompiler` 参数，用于未来非标准安装路径。

保护范围：

- Windows 安装器构建脚本：[`scripts/windows/build_installer.ps1`](scripts/windows/build_installer.ps1)
- Inno Setup 脚本：[`scripts/windows/kelivo_installer.iss`](scripts/windows/kelivo_installer.iss)

同步或重构时的检查点：

- [`scripts/windows/build_installer.ps1`](scripts/windows/build_installer.ps1) 不能退回只查找单一系统级路径。
- 用户级安装的 Inno Setup 路径 `%LOCALAPPDATA%\Programs\Inno Setup 6\ISCC.exe` 必须保持在候选路径中。
- 构建 Windows 安装程序前，应先运行 `flutter build windows` 生成 release bundle。
- 构建安装程序时应优先使用 [`scripts/windows/build_installer.ps1`](scripts/windows/build_installer.ps1)，不要在 CI 或本地流程中复制一份不同的 Inno Setup 调用逻辑。
- 输出安装包仍必须按 Release 命名规范重命名为 `JO-Kelivo-v{version}-windows-x64-setup.exe`。

### 11. 用户数据目录入口与备份导入导出文案

这是主项目保留的数据管理体验改版之一。

功能目标：

- 桌面端“备份与恢复”页面顶部必须提供“打开用户数据目录”入口。
- “存储空间”页面也必须提供“打开用户数据目录”入口，方便用户从空间占用总览直接定位主数据目录。
- 该入口打开 [`AppDirectories.getAppDataDirectory()`](lib/utils/app_directories.dart) 返回的主用户数据目录。
- 主用户数据目录用于集中查看聊天 Hive 数据、上传文件、图片、头像、缓存和日志等文件型数据。
- 该入口只承诺打开主用户数据目录；不要承诺平台 [`SharedPreferences`](pubspec.yaml) 偏好文件一定也在该目录中。
- 打开目录失败时必须有明确用户提示，不能静默失败或假成功。
- 本地备份区第一个按钮必须显示为“导出为 Kelivo 备份”。
- 本地备份区第二个按钮必须显示为“从 Kelivo 备份导入”。
- 本地备份区必须保留“从 DeepSeek 网页版/App 导入”占位按钮。
- DeepSeek 导入按钮当前只是占位入口，不应接入任何假导入逻辑、静默 fallback 或假成功流程。
- 桌面端和移动端本地备份按钮文案应保持一致。

保护范围：

- 桌面端备份设置页：[`lib/desktop/setting/backup_pane.dart`](lib/desktop/setting/backup_pane.dart)
- 移动端备份页：[`lib/features/backup/pages/backup_page.dart`](lib/features/backup/pages/backup_page.dart)
- 存储空间页：[`lib/features/settings/pages/storage_space_page.dart`](lib/features/settings/pages/storage_space_page.dart)
- 用户数据目录工具：[`lib/utils/app_directories.dart`](lib/utils/app_directories.dart)
- 本地化文案：[`lib/l10n/app_en.arb`](lib/l10n/app_en.arb)、[`lib/l10n/app_zh.arb`](lib/l10n/app_zh.arb)、[`lib/l10n/app_zh_Hans.arb`](lib/l10n/app_zh_Hans.arb)、[`lib/l10n/app_zh_Hant.arb`](lib/l10n/app_zh_Hant.arb)
- 生成本地化文件：[`lib/l10n/app_localizations.dart`](lib/l10n/app_localizations.dart)、[`lib/l10n/app_localizations_en.dart`](lib/l10n/app_localizations_en.dart)、[`lib/l10n/app_localizations_zh.dart`](lib/l10n/app_localizations_zh.dart)

同步或重构时的检查点：

- 桌面端备份页顶部不能丢失“打开用户数据目录”入口。
- 存储空间页不能丢失“打开用户数据目录”入口；移动端应在空间总览附近可见，桌面非嵌入页面也应可见。
- “打开用户数据目录”必须使用 [`AppDirectories.getAppDataDirectory()`](lib/utils/app_directories.dart) 作为唯一目录来源，不能散落硬编码 Windows、macOS、Linux、Android 或 iOS 路径。
- 打开目录失败时必须有明确用户提示，不能静默失败。
- 移动端入口只能承诺打开平台允许访问的主用户数据目录；不能伪造文件管理器行为或假成功。
- 本地备份区按钮文案不能退回“导出为文件”和“备份文件导入”。
- “从 DeepSeek 网页版/App 导入”在未实现导入服务前只能作为占位按钮，不能伪造导入成功。
- 新增或修改这些用户可见文案时，四个 ARB 文件必须同步更新，并运行 `flutter gen-l10n`。
- 修改相关 Dart UI 后，至少运行 `dart format`、`flutter analyze` 和相关备份/存储空间/本地化测试。

### 12. 宽屏聊天区域拉宽设置项

这是主项目保留的宽屏聊天布局改版之一。

功能目标：

- 移动端显示设置和桌面端显示设置里都必须保留“聊天区域拉宽”开关。
- 开关默认关闭；关闭时消息列表和输入栏继续使用当前窄宽度约束，避免改变默认阅读体验。
- 开关打开时，当前聊天页只要进入平板/宽屏聊天布局，消息列表和底部输入栏就应尽量占满可用宽度。
- 手机强制横屏后，如果逻辑宽度达到宽屏布局断点并进入宽屏聊天分支，也应允许该开关生效。
- 手机竖屏或其他窄屏布局仍不应因为开关改变布局宽度。
- 内部命名应使用中性宽屏语义，例如 [`SettingsProvider.wideChatLayout`](lib/core/providers/settings_provider.dart) 和 [`SettingsProvider.setWideChatLayout()`](lib/core/providers/settings_provider.dart)，不要再新增 desktop-only API。
- 持久化必须兼容旧 key `display_desktop_wide_chat_layout_v1`，已有用户的桌面拉宽设置不能丢失。

保护范围：

- 设置持久化：[`lib/core/providers/settings_provider.dart`](lib/core/providers/settings_provider.dart)
- 移动端显示设置页：[`lib/features/settings/pages/display_settings_page.dart`](lib/features/settings/pages/display_settings_page.dart)
- 桌面显示设置页：[`lib/desktop/setting/display_pane.dart`](lib/desktop/setting/display_pane.dart)
- 共享聊天页宽度策略：[`lib/features/home/pages/home_page.dart`](lib/features/home/pages/home_page.dart)
- 聊天宽度常量：[`lib/features/home/utils/chat_layout_constants.dart`](lib/features/home/utils/chat_layout_constants.dart)
- 消息列表横向留白策略：[`lib/features/home/widgets/message_list_view.dart`](lib/features/home/widgets/message_list_view.dart)
- 本地化文案：[`lib/l10n/app_en.arb`](lib/l10n/app_en.arb)、[`lib/l10n/app_zh.arb`](lib/l10n/app_zh.arb)、[`lib/l10n/app_zh_Hans.arb`](lib/l10n/app_zh_Hans.arb)、[`lib/l10n/app_zh_Hant.arb`](lib/l10n/app_zh_Hant.arb)
- 生成本地化文件：[`lib/l10n/app_localizations.dart`](lib/l10n/app_localizations.dart)、[`lib/l10n/app_localizations_en.dart`](lib/l10n/app_localizations_en.dart)、[`lib/l10n/app_localizations_zh.dart`](lib/l10n/app_localizations_zh.dart)
- 设置持久化测试：[`test/settings_provider_desktop_wide_chat_layout_test.dart`](test/settings_provider_desktop_wide_chat_layout_test.dart)
- 消息列表宽度测试：[`test/features/home/widgets/message_list_view_padding_test.dart`](test/features/home/widgets/message_list_view_padding_test.dart)

同步或重构时的检查点：

- “聊天区域拉宽”设置项不能从移动端或桌面端显示设置里消失，且默认值必须保持关闭。
- 设置值必须持久化，重启应用后不能丢；旧 key `display_desktop_wide_chat_layout_v1` 必须继续被读取。
- 开启后，桌面、平板、手机横屏达到宽屏断点时，宽屏聊天页消息列表横向留白应为 0，输入栏也应跟随拉宽。
- 关闭后，宽屏聊天页仍应保留当前 [`ChatLayoutConstants.maxContentWidth`](lib/features/home/utils/chat_layout_constants.dart) / [`ChatLayoutConstants.maxInputWidth`](lib/features/home/utils/chat_layout_constants.dart) 窄宽度约束。
- 窄屏手机布局不能因为该开关改变宽度约束。
- 不要再把该功能描述或实现限制为桌面专属，也不要依赖 [`HomePage.desktopChatEntry`](lib/features/home/pages/home_page.dart) 作为生效条件。
- 新增或修改这些用户可见文案时，四个 ARB 文件必须同步更新，并运行 `flutter gen-l10n`。
- 修改相关 Dart UI 后，至少运行 `dart format`、`flutter analyze`、相关布局/设置测试，并按影响范围补充移动宽屏和桌面边界自查。

### 13. 桌面托盘悬停品牌名

这是主项目保留的桌面端品牌名修复之一。

问题现象：

- 桌面端启用托盘图标后，鼠标悬停托盘图标时显示 `Kelivo`，而不是本改版品牌名 `JO-Kelivo`。
- 该问题会让 Windows / macOS 托盘或菜单栏提示仍残留原版品牌名。

修复策略：

- [`DesktopTrayController._ensureTrayIconAndMenu()`](lib/desktop/desktop_tray_controller.dart) 设置托盘 tooltip 时必须使用 `JO-Kelivo`。
- Linux 当前按现有实现跳过 tooltip 设置；不要为了品牌名修复强行改变 Linux 托盘兼容逻辑。
- [`lib/main.dart`](lib/main.dart) 中窗口初始化标题和 `MaterialApp` 标题当前仍保留 `Kelivo`，本项只保护托盘悬停文本，不扩大到全局标题重命名。

保护范围：

- 桌面托盘控制器：[`lib/desktop/desktop_tray_controller.dart`](lib/desktop/desktop_tray_controller.dart)

同步或重构时的检查点：

- [`trayManager.setToolTip()`](lib/desktop/desktop_tray_controller.dart) 不能被覆盖回 `Kelivo`。
- Windows / macOS 开启托盘图标后，鼠标悬停托盘图标应显示 `JO-Kelivo`。
- 修改托盘逻辑后，至少运行 `dart format`、`flutter analyze`，并在当前平台可用时运行对应桌面构建，例如 `flutter build windows`。

## 维护者改版规范

### 1. README 与 Release 合规信息边界

这是主项目保留的改版合规说明规则之一。

规则目标：

- 先同步写[Release日志](Release日志.md)
- 公开 [`README.md`](README.md) 必须继续明确说明 JO-Kelivo 是基于 [原版 Kelivo](https://github.com/Chevey339/kelivo) 的非官方改版，不能写成原版官方发布、维护或背书。
- 公开 [`README.md`](README.md) 必须继续保留对原版作者及贡献者的致谢，并继续说明本项目按 GNU AGPL-3.0 发布。
- 修改日期、对应上游版本、源码获取入口等发布粒度信息由 GitHub Release 承载，不要求在 [`README.md`](README.md) 中逐条重复。
- 每个对外 Release 必须能看出发布日期，并说明该版本基于哪个 [原版 Kelivo](https://github.com/Chevey339/kelivo) 版本改版。
- 每个对外 Release 分发二进制产物时，必须在同一发布页、项目仓库或清晰链接中提供对应源代码获取方式，不能只发布二进制。
- [`README.md`](README.md) 中不要放过于生硬的内部维护命令；内部同步约束应优先写入本文件。

保护范围：

- 公开项目说明：[`README.md`](README.md)
- 维护者改版记录：[`维护者改版记录.md`](维护者改版记录.md)
- 发布说明与源码入口：GitHub Release

同步或发布时的检查点：

- [`README.md`](README.md) 不能丢失“非官方改版”声明。
- [`README.md`](README.md) 不能丢失原版作者及贡献者致谢。
- [`README.md`](README.md) 不能把 JO-Kelivo 表述为原版作者发布、维护或背书的版本。
- Release notes 必须能看出该版本对应的发布时间和基于的原版版本。
- Release 发布二进制时，必须同时给出对应源码获取方式。

### 2. 改版版本号规则

这是主项目保留的自定义发布规则之一。

规则目标：

- 本项目不再直接使用上游 [原版](https://github.com/Chevey339/kelivo) 版本号。
- 同步上游源码后，必须在上游版本号后追加本项目改版序号。
- [`pubspec.yaml`](pubspec.yaml) 必须使用 Flutter 合法版本格式。

保护范围：

- 项目版本声明：[`pubspec.yaml`](pubspec.yaml)
- 本保护提醒：[`README.md`](README.md)

同步或重构时的检查点：

- [`pubspec.yaml`](pubspec.yaml) 中的 `version` 不能被 [原版](https://github.com/Chevey339/kelivo) 源码覆盖回上游版本号。
- 如未来继续同步上游版本，必须先确认新的改版版本号规则，再更新 [`README.md`](README.md) 和 [`pubspec.yaml`](pubspec.yaml)。

### 3. Android 只构建 APK

这是主项目保留的自定义发布规则之一。

规则目标：

- Android 发布产物只构建 APK。
- 永远不要构建、发布或上传 AAB。
- 本地和 CI 都应使用 `flutter build apk`，不能使用 `flutter build appbundle`。

保护范围：

- Android 构建命令和发布流程：[`android/`](android/)、[`.github/workflows`](.github/workflows)
- 本保护提醒：[`README.md`](README.md)

同步或重构时的检查点：

- 构建 Android 时只能生成 APK 产物。
- 如发现 `flutter build appbundle`、`.aab` 发布上传、或任何 AAB 专用流程，必须删除或改为 APK 流程。
- 当前推荐命令为 `flutter build apk --release --split-per-abi`。

### 4. Release 二进制文件命名规范

这是主项目保留的自定义发布规则之一。

规则目标：

- 对外发布的二进制文件必须使用统一、可读、可长期归档的命名格式。
- 文件被单独下载后，必须仍能从文件名看出项目、版本、平台、架构和分发形态。
- 历史归档目录继续按版本号分目录，例如 [`历史Release二进制文件/0.1.0+0`](历史Release二进制文件/0.1.0+0)，文件名内部使用带 `v` 前缀的完整版本号。

统一格式：

```text
JO-Kelivo-v{version}-{platform}-{arch}-{package}.{ext}
```

字段规则：

- `JO-Kelivo`：发行品牌名，必须与 Release 标题保持一致。
- `v{version}`：完整 Flutter 版本号，来自 [`pubspec.yaml`](pubspec.yaml) 的 `version`，例如 `v0.1.0+0`。
- `{platform}`：固定小写，只使用 `android`、`windows`、`macos`、`linux`、`ios`。
- `{arch}`：目标架构或通用架构组合，例如 `arm64-v8a`、`armeabi-v7a`、`x86_64`、`x64`、`arm64`、`universal`。
- `{package}`：分发形态或包类型，例如 `release`、`setup`、`portable`、`dmg`、`app`、`appimage`、`tar`、`deb`、`rpm`、`archive`。
- `{ext}`：真实文件扩展名，不能为了统一而伪造扩展名。

平台示例：

| 平台 | 示例 |
| --- | --- |
| Android APK 单 ABI | `JO-Kelivo-v0.1.0+0-android-arm64-v8a-release.apk` |
| Android APK 单 ABI | `JO-Kelivo-v0.1.0+0-android-armeabi-v7a-release.apk` |
| Android APK 单 ABI | `JO-Kelivo-v0.1.0+0-android-x86_64-release.apk` |
| Android 通用 APK | `JO-Kelivo-v0.1.0+0-android-universal-release.apk` |
| Windows 安装包 | `JO-Kelivo-v0.1.0+0-windows-x64-setup.exe` |
| Windows 便携包 | `JO-Kelivo-v0.1.0+0-windows-x64-portable.zip` |
| macOS Intel DMG | `JO-Kelivo-v0.1.0+0-macos-x64-dmg.dmg` |
| macOS Apple Silicon DMG | `JO-Kelivo-v0.1.0+0-macos-arm64-dmg.dmg` |
| macOS Universal DMG | `JO-Kelivo-v0.1.0+0-macos-universal-dmg.dmg` |
| macOS app 压缩包 | `JO-Kelivo-v0.1.0+0-macos-universal-app.zip` |
| Linux AppImage | `JO-Kelivo-v0.1.0+0-linux-x64-appimage.AppImage` |
| Linux tar 包 | `JO-Kelivo-v0.1.0+0-linux-x64-tar.gz` |
| Linux deb 包 | `JO-Kelivo-v0.1.0+0-linux-x64-deb.deb` |
| Linux rpm 包 | `JO-Kelivo-v0.1.0+0-linux-x64-rpm.rpm` |
| iOS IPA | `JO-Kelivo-v0.1.0+0-ios-universal-release.ipa` |
| iOS archive | `JO-Kelivo-v0.1.0+0-ios-universal-archive.xcarchive.zip` |

校验文件规则：

- 校验文件必须使用完整产物文件名再追加算法后缀。
- 推荐优先使用 `sha256`，例如 `JO-Kelivo-v0.1.0+0-windows-x64-setup.exe.sha256`。
- 如果保留旧版 `sha1`，也必须使用同样规则，例如 `JO-Kelivo-v0.1.0+0-android-arm64-v8a-release.apk.sha1`。

同步或发布时的检查点：

- 新增 Release 二进制前，先确认文件名符合统一格式。
- 同一 Release 中不同平台、架构和包类型不能使用模糊名称，如 `app-release.apk`、`setup.exe`、`latest.zip`。
- Android 仍只发布 APK，不发布 AAB；即使示例中出现其他包类型，也不能覆盖“Android 只构建 APK”的规则。
- 重命名二进制文件时，必须同步重命名对应校验文件。
- Release notes 中如列出下载文件，必须使用重命名后的文件名。

### 5. 新版本检测来源固定为 JO-Kelivo

这是主项目保留的改版发布规则之一。

问题现象：

- 如果继续使用原版 Kelivo 的更新源，JO-Kelivo 会检测到原始 Kelivo 的上游版本，导致用户收到错误的新版本提示。
- JO-Kelivo 的更新提示必须只面向本项目自己的 Release，不能跟随原版 Kelivo 更新源。

规则目标：

- 新版本检测必须请求 JO-Kelivo 自己的 GitHub Releases 最新正式版。
- 更新源固定为 `https://api.github.com/repos/JO-Beacon/JO-Kelivo/releases/latest`。
- 解析版本时使用 GitHub Release 的 `tag_name`，允许移除开头的 `v` / `V` 后再与 [`pubspec.yaml`](pubspec.yaml) 当前版本比较。
- 下载链接必须从 Release assets 中按平台匹配真实安装包或压缩包。
- 暂不做下载后的 hash 校验；`.sha1` / `.sha256` 文件只作为 Release 附属校验文件，不能被当作用户点击下载的更新包。

保护范围：

- 更新检测 provider：[`lib/core/providers/update_provider.dart`](lib/core/providers/update_provider.dart)
- 更新提示入口：[`lib/features/home/widgets/side_drawer.dart`](lib/features/home/widgets/side_drawer.dart)
- 更新检测测试：[`test/update_provider_test.dart`](test/update_provider_test.dart)

同步或重构时的检查点：

- [`UpdateProvider.checkForUpdates()`](lib/core/providers/update_provider.dart) 不能回退到 `https://kelivo.psycheas.top/update.json` 或其他原版 Kelivo 更新源。
- [`UpdateInfo.fromGitHubRelease()`](lib/core/providers/update_provider.dart) 必须继续解析 GitHub Release 格式，而不是原版 `update.json` 格式。
- Release assets 匹配必须只接受 `JO-Kelivo` 产物名，避免匹配到原版 `kelivo` 资产。
- `.sha1` 和 `.sha256` 不能作为平台下载链接；除非后续实现完整的下载、计算 hash、比对和失败提示流程，否则不要把校验文件纳入更新跳转目标。
- Android 下载优先 `arm64-v8a` APK；Windows 优先安装包 `exe`，没有安装包时再使用便携 `zip`；Linux 优先 `AppImage`，再考虑 `deb`、`rpm`、`tar.gz`；macOS 使用 `dmg`。
- 修改更新检测逻辑后，至少运行 [`test/update_provider_test.dart`](test/update_provider_test.dart)、`flutter analyze` 和相关测试。

### 6. 本地依赖与工作区边界

这是主项目保留的同步边界之一。

规则目标：

- Markdown 渲染依赖必须使用 [原版](https://github.com/Chevey339/kelivo)  1.1.13 随源码带来的本地路径依赖，而不是回退到 pub.dev 版本。
- [`pubspec.yaml`](pubspec.yaml) 中的 `gpt_markdown` 必须指向 [`dependencies/gpt_markdown`](dependencies/gpt_markdown)。
- 本地安装器脚本和本地快捷方式不能被误提交。

保护范围：

- 本地 Markdown 依赖：[`dependencies/gpt_markdown`](dependencies/gpt_markdown)
- 依赖声明：[`pubspec.yaml`](pubspec.yaml)
- 忽略规则：[`.gitignore`](.gitignore)

同步或重构时的检查点：

- 不要把 `gpt_markdown` 回退成 pub.dev 版本约束。
- 不要删除 [`dependencies/gpt_markdown`](dependencies/gpt_markdown) 本地依赖目录。
- [`.gitignore`](.gitignore) 必须继续忽略 `installer.iss` 和本地快捷方式。

白话风险提醒：

1. 以后别把 [`gpt_markdown`](pubspec.yaml) 改回网上下载版。
2. 以后别把 [`dependencies/gpt_markdown`](dependencies/gpt_markdown) 里的相对导入覆盖回包名导入。
3. 以后别删 [`.gitignore`](.gitignore) 里忽略本地文件的规则。

### 7. 同步 [原版](https://github.com/Chevey339/kelivo) 代码前的最低检查

每次从参考源码或上游版本同步前，至少检查：

1. [`README.md`](README.md) 是否仍是当前保护提醒版本，并保留非官方改版声明、原版作者及贡献者致谢、GNU AGPL-3.0 发布说明。
2. 单条聊天记录身份切换是否仍存在。
3. 新建或复制助手插到顶部设置项是否仍存在。
4. [`pubspec.yaml`](pubspec.yaml) 是否仍使用本项目改版版本号，而不是上游 [原版](https://github.com/Chevey339/kelivo) 版本号。
5. Android 构建和发布流程是否仍只生成 APK，且没有引入 AAB。
6. Release 二进制文件名是否仍符合 `JO-Kelivo-v{version}-{platform}-{arch}-{package}.{ext}` 规范，校验文件是否同步使用完整产物名追加算法后缀。
7. 创建分支时的克隆数据完整性是否仍保留，消息身份、版本组、元数据、工具事件和 thought signature 不能丢。
8. 创建分支时的源消息选择是否仍按目标消息原始下标连续截取，不能回退为按 `groupId` 集合筛选。
9. [`gpt_markdown`](dependencies/gpt_markdown) 是否仍使用本地路径依赖，不能回退为 pub.dev 版本约束。
10. 长会话版本消息修复是否仍保留，包括 [`ChatController.collapseVersions()`](lib/features/home/controllers/chat_controller.dart) 的稳定排序与窗口过滤逻辑、[`ChatController.reloadMessages()`](lib/features/home/controllers/chat_controller.dart) 删除分支后的懒加载开关分流、[`ChatService`](lib/core/services/chat/chat_service.dart) 对新版本消息插回同组附近的写入逻辑，以及 [`repair_chat_archive/`](repair_chat_archive/) 独立旧存档修复工具。
11. DeepSeek Anthropic 通道与原生搜索修复是否仍保留，包括默认 DeepSeek provider 类型、默认 `https://api.deepseek.com/anthropic`、普通内置搜索支持、禁止 DeepSeek 显示“模型内置搜索(新)”，以及 Anthropic server tool 在 `end_turn` 后不能重复续轮。
12. 历史消息附件可视化编辑是否仍保留，包括附件标记解析、附件区 UI、删除/添加/替换附件、保存为新版本以及旧格式兼容。
13. 用户消息图片分离显示设置是否仍保留，包括默认关闭、设置持久化、气泡内/气泡外两种展示模式，以及不修改 `[image:path]` 存档格式。
14. 桌面端聊天区域拉宽设置是否仍保留，包括默认关闭、设置持久化、仅桌面入口生效、消息列表和输入栏同步拉宽。
15. 新版本检测是否仍固定到 JO-Beacon/JO-Kelivo GitHub Releases 最新正式版，不能回退到原版 Kelivo 的 `kelivo.psycheas.top/update.json`；`.sha1` / `.sha256` 仍不能作为用户点击下载的更新包。
16. [`.gitignore`](.gitignore) 是否仍忽略本地安装器和本地快捷方式。
17. 如果同步覆盖了 ARB 文件，必须重新补齐四个语言文件并运行本地化生成。
18. Release 是否写明对应上游版本和发布时间；如发布二进制，是否同时提供对应源码获取方式和规范文件名。
19. 如果同步覆盖了 Dart 代码，必须重新运行格式化、分析和测试。