# JO-Kelivo 维护者改版记录

本文件记录当前已经落地且需要长期保护的 JO 差异、外部基座来源和兼容边界。它是维护清单，不是愿望清单；过程性计划位于本地 `plans/`，不进入 Git，也不作为当前实现的事实来源。

## 1. 当前基线

- JO 产品版本：`0.1.6+6`。
- 当前实现基座：原版 Kelivo `1.2.1+64`。
- 来源仓库：`https://github.com/Chevey339/kelivo`。
- 来源 tag：`v1.2.1`。
- 来源 commit：`dae00af67681242f820ddfb9c7ea9ead35dcab5b`。
- 许可证：GNU AGPL-3.0；继续保留原作者及贡献者归属。
- Dart package name：`Kelivo`，现有导入继续使用 `package:Kelivo/...`。

JO-Kelivo 保持独立 Git 历史。外部基座是可替换的实现输入，不是 JO 仓库的祖先或永久上游；替换基座不得隐式 rebase、桥接无关历史、改变 remote 或整树复制外部工作区。

本轮的正式只读输入是 `参考文件/原版kelivo1.2.1/`。`C:\Users\27548\Desktop\kelivo` 仅用于查看原版主开发线的 changelog、diff 和后续修复思路，不是复制源，也不能让固定的 `v1.2.1` 基座静默漂移。

## 2. 基座替换边界

### 2.1 接受的路径组

以下内容以 Kelivo 1.2.1 为架构和行为基准，再按 JO 要求做最小适配：

- `lib/`：SQLite/Drift 数据库、repository、结构化 `MessagePart`、备份恢复、迁移、provider、语音、MCP、记忆、搜索和 UI 主体。
- `test/`、`integration_test/`、`test_driver/`、`tool/`：对应上游实现的测试、恢复工具和性能基线。
- `android/`、`ios/`、`macos/`、`linux/`、`windows/`、`web/`：平台模板、插件接入和 1.2.1 平台能力；应用身份部分由 JO 覆盖。
- `dependencies/`：1.2.1 所需本地 path dependencies，包括 `downsize` 和更新后的 MCP client。
- `assets/`、`docs/`、`docx/`、`drift_schemas/`：实际被运行时、文档或数据库验证使用的基座资源。
- `pubspec.yaml`、`analysis_options.yaml`、`l10n.yaml` 等根级构建配置：以 1.2.1 依赖和工具链为主，保留 JO 版本、仓库隔离和发布规则。

### 2.2 JO 所有或混合路径

以下内容不能被外部快照直接覆盖：

- `AGENTS.md`：仓库硬约束和可替换基座规则。
- `README.md`、本文件、`Release日志.md`：JO 产品、维护和发布记录。
- `.github/workflows/**`、`scripts/windows/**`：JO 发布矩阵、签名边界和产物命名。
- 平台应用标识、显示名、URL scheme、Windows 单实例标识、数据目录来源、About、更新源和 Release asset 匹配。
- `optimize_chat_archive/`：仅面向 JO 0.1.5 及更早旧 `chats.json` 的独立工具。
- JO 功能触及的数据库、provider、设置和 UI 文件：先采用 1.2.1 架构，再按需求回放，不恢复旧文件。

### 2.3 明确排除

- 外部 `.git/**`、`.dart_tool/**`、`build/**`、IDE 配置、个人环境文件、签名材料和 secrets。
- 外部仓库无关 workflow、发布文档和产品身份文件。
- `参考文件/**` 本身；它只读且不进入 Git。
- `plans/**`；新增计划永久不跟踪。历史上已跟踪的 7 个旧文件留待单独清理任务处理。
- 上游已经删除且当前无引用、无迁移价值的旧资产或实现。

常规生成物必须由仓库命令生成。`lib/core/models/chat_message.g.dart` 是 1.2.1 上游手工维护的旧 Hive 读取适配器，field 2 到结构化正文的迁移映射必须保留，不能被无条件 build_runner 覆盖。

## 3. 1.2.1 架构边界

### 3.1 数据与消息

- 聊天运行时唯一真相是 SQLite/Drift；旧 Hive 只参与首次迁移，不继续双写。
- 消息正文和附件唯一真相是 `List<MessagePart>`；图片、文件、文本、推理和未知 part 按结构化数据处理。
- `[image:]` / `[file:]` 只用于旧数据和旧备份解码，以及迁移前恢复备份；JO 0.1.6 正常运行和正常新备份不得写回 marker。
- 更新消息角色、正文或附件必须经过 repository 和事务路径，不能只修改内存对象。
- 长会话继续使用数据库窗口化和稳定排序；完整查看是受控读取路径，不把全历史永久塞入常驻内存。

### 3.2 基座能力保护

下列 1.2.1 能力不得被旧 JO 文件覆盖：

- SQLite 安装、观察、备份、恢复、迁移收尾和失败回滚。
- 结构化 parts、附件资产引用、未知或损坏 part 的保真处理。
- 旧 Hive/`chats.json` 清洗、损坏记录跳过、计数和恢复备份。
- 记忆系统、语音与 ASR、MCP OAuth、本地设备工具和平台权限。
- Azure TTS、Gboard 图片粘贴、豆包搜索、话题复制及 1.2.1 provider 修复。
- data URI 图片缓存字节上限、工具详情动画、Responses 推理与图片生成兼容、iOS 合规和 Markdown 修复。

未被 JO 适配触及的模块以 `v1.2.1` 标签源码及上游测试为主要证据；触及的模块必须运行上游测试和 JO 差异测试。

## 4. JO 身份与发布规则

### 4.1 应用与数据隔离

- Android namespace/application ID：`com.psyche.jokelivo`。
- iOS/macOS Bundle ID：`com.psyche.jokelivo` 及对应测试 target 变体。
- Windows/Linux binary：`jo_kelivo`。
- Linux application ID：`com.psyche.jokelivo`。
- Web、窗口、托盘和系统显示名：`JO-Kelivo`。
- OAuth scheme 使用 JO 标识；`kelivo-file:///` 和既有兼容 URI 标识继续保留。
- Windows 单实例 mutex：`JOKelivoMutex`，窗口唤起只匹配 JO 实例。
- 运行时目录由 JO 平台身份和 `path_provider` 路径解析得到；不得 fallback 到原版 Kelivo 数据目录。
- 桌面备份页和存储空间页保留两个不同入口，但都打开当前 JO 主数据目录。

在 JO 身份和数据目录恢复完成前，不得启动新基座或触发旧数据迁移。首次迁移试验不得直接使用用户真实数据。

### 4.2 About、更新和版本

- About 的 JO 分区显示 `0.1.6+6`，原版归属分区显示基座 `1.2.1 / 64`。
- 更新源固定为 `https://api.github.com/repos/JO-Beacon/JO-Kelivo/releases/latest`。
- Release tag 使用产品版本 `v0.1.6`；正式资产名包含完整 Flutter 版本 `0.1.6+6`。
- 更新匹配只接受当前 tag 对应的 `JO-Kelivo` 资产，拒绝原版资产、其他版本和 `.sha1`/`.sha256`。
- Android 优先 arm64-v8a，Windows 优先 setup 后 portable，Linux 优先 AppImage 后 deb/tar.gz。

### 4.3 发布矩阵

JO 候选发布矩阵保持：

- Android：arm64-v8a、armeabi-v7a、x86_64 三个 release APK。
- Windows x64：portable ZIP、setup EXE。
- Linux x64：AppImage、tar.gz、deb。
- 每个二进制资产都有同名 `.sha256`。

非发布 `workflow_dispatch` 会给资产版本追加短 commit，以免测试 artifact 冒充正式资产；只有 `v0.1.6` tag 或显式 `publish_release=true` 且 `release_tag=v0.1.6` 才进入 GitHub Release 步骤。Android 发布必须具备完整签名 secrets；本地 keystore 和 `key.properties` 永不提交。

Windows workflow 当前没有 Authenticode 签名步骤，因此不得声称 Windows EXE 已签名。发布前必须按实际产物记录签名状态。

## 5. JO 功能处置

| 能力 | 当前处置 | 当前实现边界 |
| --- | --- | --- |
| 单条消息用户/模型身份切换 | 替换实现 | repository 事务更新角色；刷新、重启和导出后保持一致，parts 不丢失 |
| 新建/复制助手置顶 | 回放 | 适配 1.2.1 business repository 和排序模型，移动端与桌面端共用规则 |
| 长会话懒加载与完整查看 | 替换实现 | 保留设置；关闭时走受控完整数据库读取，失败恢复原状态 |
| 长会话版本顺序 | 替换实现 | 依赖 1.2.1 稳定排序、版本选择和分页不变量，不恢复 Hive `messageIds` 写入补丁 |
| 历史附件可视化编辑 | 替换实现 | 编辑 `MessagePart`，支持增删替换附件并保留未知 part |
| 宽屏聊天区域拉宽 | 回放 | 落在共享聊天内容层，不改写 desktop shell 路由 |
| 两个用户数据目录入口 | 回放 | 桌面备份页和存储空间页保留各自入口语义，目标均为 JO 主目录 |
| DeepSeek 默认 Anthropic-compatible | 回放 | 新建默认配置为 `ProviderKind.claude` 和 `https://api.deepseek.com/anthropic` |
| DeepSeek 内置搜索 | 保留基座并接通 JO 默认 | 使用 1.2.1 的识别、`web_search_20250305`、流式解析和 `end_turn` 行为 |
| DeepSeek 显式 OpenAI-compatible `/v1` | 保留 | 只在用户明确配置时使用，不被新默认覆盖 |
| 供应商余额查询 | 回放 | 非 OpenAI-compatible 主端点不静默拼接相对余额路径 |
| DeepSeek Web/App 导入入口 | 保留占位 | 继续显示“暂不支持”，点击不产生任何导入数据；网页版实装属于后续任务 |
| 旧存档优化工具 | 限定保留 | 只接受 JO 0.1.5 及更早导出的旧 `chats.json`，拒绝 Hive、SQLite、其他备份和非目标 JSON |
| 用户消息图片位置开关 | 退役 | 采用 1.2.1 固定显示规则；遗留 key 不得导致崩溃或重新出现无效 UI |

## 6. 不得恢复的旧实现

- Hive 作为聊天运行时数据库，或 SQLite/Hive 双写。
- 把附件重新编码进正文 marker 并与 parts 双写。
- 绕过 repository 直接修改消息角色、正文或版本顺序。
- 为完整查看永久加载全部会话并绕开数据库窗口。
- 1.1.16 时代基于 `Conversation.messageIds` 的写入层插回补丁。
- 旧独立附件解析器作为日常数据真相，或要求新备份向 JO 0.1.5 降级。
- 已退役的用户消息图片位置设置和渲染分支。

旧 `chats.json` 优化工具仍可修复历史导出文件，但它不能读写当前应用数据库，也不能接入启动、打开会话或备份导入流程。

## 7. 数据兼容

### 7.1 明确支持

- 已安装 JO 0.1.5 从原 JO 数据目录首次升级到 JO 0.1.6。
- JO 0.1.5 备份导入干净安装的 JO 0.1.6。
- 可解码的会话、消息、版本关系、文本、图片和文件附件迁移到 1.2.1 数据模型。
- JO 0.1.5 供应商和设置迁移到 business repository；既有 DeepSeek provider、base URL、API key、模型和 model overrides 不被新建默认值覆盖。
- 迁移失败保留恢复备份和可诊断状态；损坏记录按上游规则跳过并计数，不伪装成完整迁移。

### 7.2 不承诺

- JO 0.1.6 正常备份被 JO 0.1.5 完整导入。
- JO 0.1.6 正常备份被原版 Kelivo 完整反向导入。
- 新结构化附件降级写回 marker 供旧版本继续编辑。
- 旧 Hive 在迁移后继续作为运行时数据库。

兼容测试以合成 JO 0.1.5 夹具和上游迁移测试为证据；没有脱敏真实用户存档不阻塞候选，但发布记录必须说明样本边界。

## 8. 验证基线

截至 2026-08-14，本轮已取得的主要验证结果：

- `flutter analyze`：通过。
- 完整 `flutter test`：2317 passed，6 skipped，0 failed。
- 数据库、迁移和恢复专项：576 passed，5 skipped，0 failed。
- parts/provider 专项：110 passed。
- 平台/MCP 专项：96 passed。
- Windows 适配专项：111 passed，2 skipped。
- MCP path dependency：`dart analyze` 通过；VM 测试 98 passed。默认 `dart test` 需要 Chrome，本机未覆盖浏览器平台。
- `dependencies/downsize`：`dart analyze` 通过，3 tests passed。
- Windows release 构建、portable 和 setup 打包、SHA-256、内部 EXE 名称与版本信息：通过。
- PR 干净检出：生成空 fallback 后 analyze 和相关测试通过；发布 workflow 仍从 GitHub secret 注入 fallback。
- `desiredFileName.txt`：`{}`。

当前发布候选边界：

- Windows 两个候选已真实构建；主 EXE 和安装器均未做 Authenticode 签名。
- Android release 构建在下载 Google Maven 依赖时因 `dl.google.com:443` 超时中断，尚无本轮三个 APK 候选；不是源码编译失败，也不能记为通过。
- 当前 Windows 机器没有 WSL Linux 发行版或 Docker，尚未真实构建本轮 Linux 三种候选。
- iOS、macOS、Web 不是本轮候选发布平台，未做 release 构建。
- 未使用真实 DeepSeek key 做在线冒烟；强制协议验收使用本地模拟服务完成。
- 本地新 workflow 尚未位于远端可执行 ref，因此未触发 GitHub Actions artifact 上传；已通过 Actions linter 和仓库发布契约测试验证上传路径与 Release 门控。迁移分支推送后仍需执行一次非发布 dispatch。

上述未覆盖项必须在最终发布决策中保持可见。只有完整候选矩阵真实构建、校验和上传流程验证完成后，才能声称跨平台发布门禁全部通过。

## 9. 后续维护检查

每次替换基座或修改相关模块时至少检查：

1. 基座来源、版本、commit 和许可证记录是否准确，JO 历史和 remote 是否保持独立。
2. 平台身份、数据目录、OAuth scheme、Windows 单实例、托盘、About 和更新源是否仍为 JO。
3. 聊天运行时是否仍只有 SQLite/Drift 一套真相，消息是否仍以 parts 为真相。
4. JO 功能处置表中的回放、替换和退役结论是否被代码与需求测试保护。
5. 四个 ARB、生成文件、path dependencies 和相似 workflows 是否按 `AGENTS.md` 同步验证。
6. 发布资产、`.sha256`、tag、版本、签名状态和更新匹配是否与实际产物一致。
7. `plans/**`、参考快照、secrets、签名材料和构建产物是否未进入暂存区。

长期记录只写已经实现并验证的事实。尚未实现的 DeepSeek 网页版导入等事项留在本地计划或独立 issue，不得提前写成产品能力。
