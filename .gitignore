# Miscellaneous
*.class
*.log
*.pyc
*.swp
.DS_Store
.atom/
.build/
.claude/
.zcode/
.buildlog/
.history
.svn/
.swiftpm/
migrate_working_dir/

# IntelliJ related
*.iml
*.ipr
*.iws
.idea/

# The .vscode folder contains launch configuration and tasks you configure in
# VS Code which you may wish to be included in version control, so this line
# is commented out by default.
#.vscode/

# Flutter/Dart/Pub related
**/doc/api/
**/ios/Flutter/.last_build_id
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.pub-cache/
.pub/
/build/

# 图标派生工具链的 Node 依赖（scripts/icons 下执行 npm install 后产生）
scripts/icons/node_modules/

# Symbolication related
app.*.symbols

# Obfuscation related
app.*.map.json

# Android Studio will place build artifacts here
/android/app/debug
/android/app/profile
/android/app/release

# 安卓 release 签名
/android/key.properties
/android/app/key.properties
/android/app/kelivo-release.jks
/android/app/*.jks
/android/app/*.keystore

# 依赖锁文件：本项目是应用（不是被引用的库），根目录这一份有意纳入版本控制，
# 这样云端构建与本机才会用完全相同的依赖版本（Dart 官方建议应用包提交锁文件）。
# 先排除所有层级，再单独放行根目录那一份；dependencies/ 下的 fork 包是库，
# 各自的锁文件继续排除。
pubspec.lock
!/pubspec.lock
/linux/flutter/generated_plugin_registrant.cc
/windows/flutter/generated_plugin_registrant.cc
/linux/flutter/generated_plugin_registrant.h
/windows/flutter/generated_plugin_registrant.h
/linux/flutter/generated_plugins.cmake
/windows/flutter/generated_plugins.cmake
/macos/flutter/GeneratedPluginRegistrant.swift

internationalization.md
/macos/Podfile.lock
/desiredFileName.txt
.claude/settings.local.json
/.codex/
/.agent/
/.agents/
/skills-lock.json
/.grok/
/.workbuddy/

# Local release artifacts
/dist/
/JO-Kelivo-v*-windows-*-portable.zip
/JO-Kelivo-v*-windows-*-setup.exe
/JO-AIClient-v*-windows-*-portable.zip
/JO-AIClient-v*-windows-*-setup.exe
/build/installer-languages/

/参考文件/
/plans/

# Local agent instruction mirrors; keep AGENTS.md and CLAUDE.md out of Git.
/AGENTS.md
/CLAUDE.md

# Flutter 工具本地状态文件
.flutter
.flutter_tool_state

# 历史 Release 二进制存档（仅本地保留）
/历史Release二进制文件/

# 临时诊断文件（本地备查，不进仓库）
# 只屏蔽具体文件，不要屏蔽 test/goldens/ 整个目录——金标测试的标准答案图
# 必须随代码入库，否则换机器跑测试会失败。
# 注：滚动条探针（含其金标图）已于 2026-09-11 挪到 build/probes/，这两条规则
# 保留作防御——万一有诊断文件被放回 test/，仍不会被误提交。
test/tmp_scrollbar_probe_test.dart
test/goldens/dialog_scrollbar_probe.png

# 上游 1.2.7 引入 / 本仓库一并采用
# proot 的原生库由构建时 tool/fetch_proot.sh 拉取，不入库
android/app/src/main/jniLibs/**/*.so

# Local iOS profiling tools and device measurements
/docs/performance/ios-device-*/
/tool/ios_performance/
